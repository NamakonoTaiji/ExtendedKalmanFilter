--#################################################################
--# 3. 補助関数
--#################################################################
function averageObservations(buffer) --[[ ... 実装変更なし ... ]] end

function rotateLocalToGlobal_UserA(pitch, yaw, roll, Lx, Ly, Lz) --[[ ... 実装変更なし ... ]] end

--[[ (初期化はトップレベル変数定義 & プロパティ読み込みで行われる) ]]
